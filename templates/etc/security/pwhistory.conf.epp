<%-|
  Boolean              $debug,
  Boolean              $enforce_for_root,
  Integer[0]           $remember,
  Integer[0]           $retry,
  StdLib::Absolutepath $remember_file
|-%>
# This file is generated by Puppet
# Any changes made to it will be overwritten.
#
<% if $debug  { -%>
debug
<% } -%>
<% if $enforce_for_root  { -%>
enforce_for_root
<% } -%>
remember = <%= $remember %>
retry = <%= $retry %>
file = <%= $remember_file %>
